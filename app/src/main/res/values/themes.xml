<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="SplashTheme" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.MagicAndroidApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryDark"> @color/deep_black</item>
        <item name="colorPrimary">@color/orange_deep</item>
    </style>

    <style name="Theme.MagicAndroidApp" parent="Base.Theme.MagicAndroidApp" />
</resources>